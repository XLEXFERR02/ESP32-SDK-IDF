#include <stdio.h>

int main()
{
    int n = 8;
    int *p;
    p = &n;
    printf("El valor de la dirección de n es : %p \n", &n);
    printf("El valor de la dirección de n es : %p \n", p);
    
    printf("El valor de n es : %d \n", n);
    printf("El valor de n es : %d \n", *p);
    return 0;
}
